{
  "$comment": "The consumer contract, as data. One entry per thing a reusable workflow reaches for in the calling repository. This table is the single source for three consumers: bin/check-consumer-contract.mjs (the doctor), docs/adopting-an-existing-repo.md (generated from it), and test/consumer-contract.bats (which holds it against docs/consumer-guide.md's own tables). Adding a requirement to a workflow without adding it here is the drift this file exists to prevent.",

  "$schema-notes": {
    "kind": "How to check it. package-script: package.json scripts[target]. package-dep: a dependency or devDependency named target. script-or-dep: either (knip, which is deliberately a binary and not a script). file: any one of target exists. dir-nonempty: target is a directory with at least one entry. mise-tool: a mise config declares [tools] target. ignores-workflows: target's config text excludes the .workflows checkout. ignores-workflows-or-narrow: the same, but also satisfied when the config declares no tree-wide (**/) glob, so nothing of ours is in its reach to begin with. caller-path: an input in the caller names a consumer-relative path, which must then exist.",
    "toggle": "The workflow_call input that decides whether this is needed at all, as `workflow.yml:input`. The doctor reads the caller's own `with:` block first and falls back to defaultOn.",
    "severity": "required: the gate cannot run without it and the job dies. degrades: shared-workflows has a fallback, so the gate still runs but not as the consumer defined it. optional: nothing happens unless the consumer opts in.",
    "guide": "Anchor in docs/consumer-guide.md."
  },

  "profiles": ["checks", "unit", "e2e", "web", "badges", "codeql", "security", "release"],

  "requirements": [
    {
      "id": "toolchain.mise",
      "kind": "mise-tool",
      "target": ["node", "pnpm"],
      "profile": "checks",
      "neededBy": "the setup action, in every job of every workflow",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Add a .mise.toml with a [tools] table declaring at least node and pnpm. jdx/mise-action installs from it, and with no config it installs nothing - the first symptom is 'missing command: pnpm' several steps later.",
      "guide": "60-second-start"
    },
    {
      "id": "toolchain.mise-ruby",
      "kind": "mise-tool",
      "target": ["ruby"],
      "profile": "checks",
      "neededBy": "check-code.yml (release-checks), and every fastlane lane workflow",
      "toggle": "check-code.yml:release-checks",
      "defaultOn": false,
      "severity": "required",
      "fix": "Add ruby to your .mise.toml [tools] table. scripts/ci/ruby-version.sh reads the pin from there and hands it to ruby/setup-ruby.",
      "guide": "check-codeyml"
    },
    {
      "id": "toolchain.package-json",
      "kind": "file",
      "target": ["package.json"],
      "profile": "checks",
      "neededBy": "every script gate, through scripts/checks/run-script.sh",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "This family runs a consumer's package.json scripts by name. A repository without one cannot satisfy any gate.",
      "guide": "script-contract"
    },
    {
      "id": "toolchain.pnpm-lock",
      "kind": "file",
      "target": ["pnpm-lock.yaml"],
      "profile": "checks",
      "neededBy": "the setup action (pnpm install --frozen-lockfile), and scripts/ci/native-hash.sh",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Run pnpm install and commit pnpm-lock.yaml. This family is pnpm-only: there is no npm or yarn path, and the native cache key is read straight out of the lockfile before any install runs.",
      "guide": "60-second-start"
    },

    {
      "id": "script.typecheck",
      "kind": "package-script",
      "target": "typecheck",
      "profile": "checks",
      "neededBy": "check-code.yml (typecheck)",
      "toggle": "check-code.yml:typecheck",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add a \"typecheck\" script, or pass typecheck: false in your caller.",
      "guide": "script-contract"
    },
    {
      "id": "script.lint",
      "kind": "package-script",
      "target": "lint",
      "profile": "checks",
      "neededBy": "check-code.yml (lint)",
      "toggle": "check-code.yml:lint",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add a \"lint\" script, or pass lint: false in your caller.",
      "guide": "script-contract"
    },
    {
      "id": "script.format-check",
      "kind": "package-script",
      "target": "format:check",
      "profile": "checks",
      "neededBy": "check-code.yml (format)",
      "toggle": "check-code.yml:format",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add a \"format:check\" script, or pass format: false in your caller.",
      "guide": "script-contract"
    },
    {
      "id": "script.knip",
      "kind": "script-or-dep",
      "target": "knip",
      "profile": "checks",
      "neededBy": "check-code.yml (knip)",
      "toggle": "check-code.yml:knip",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add knip as a devDependency - run-script.sh falls through to the binary. Do NOT add a package.json script literally named \"knip\": that fails expo-doctor's package.json check, which check-code.yml also runs. Or pass knip: false in your caller.",
      "guide": "script-contract"
    },
    {
      "id": "script.spell",
      "kind": "package-script",
      "target": "spell",
      "profile": "checks",
      "neededBy": "check-code.yml (spell)",
      "toggle": "check-code.yml:spell",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add a \"spell\" script, or pass spell: false in your caller.",
      "guide": "script-contract"
    },
    {
      "id": "script.check-docs",
      "kind": "package-script",
      "target": "check:docs",
      "profile": "checks",
      "neededBy": "check-code.yml (docs-check)",
      "toggle": "check-code.yml:docs-check",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add a \"check:docs\" script deciding what 'docs are in order' means for your repository, or pass docs-check: false in your caller. This is a house-invented name rather than a convention, so it is the most common first red on adoption.",
      "guide": "moving-to-a-version-that-added-docs-check"
    },
    {
      "id": "script.deps-licenses",
      "kind": "package-script",
      "target": "deps:licenses",
      "profile": "checks",
      "neededBy": "check-code.yml (licenses)",
      "toggle": "check-code.yml:licenses",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add a \"deps:licenses\" script enforcing your dependency licence policy, or pass licenses: false in your caller. Another house-invented name with no fallback.",
      "guide": "script-contract"
    },
    {
      "id": "script.deps-check",
      "kind": "package-script",
      "target": "deps:check",
      "profile": "checks",
      "neededBy": "check-code.yml (expo-doctor)",
      "toggle": "check-code.yml:expo-doctor",
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Without a \"deps:check\" script, shared-workflows runs its own scripts/checks/expo-doctor.sh instead. That runs expo-doctor alone, where a consumer script usually also runs `expo install --check`. The gate you define and the gate CI runs should be the same gate.",
      "guide": "script-contract"
    },
    {
      "id": "script.deps-audit",
      "kind": "package-script",
      "target": "deps:audit",
      "profile": "checks",
      "neededBy": "check-code.yml (audit)",
      "toggle": "check-code.yml:audit",
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Without a \"deps:audit\" script, shared-workflows runs its own scripts/checks/audit.sh, which is `pnpm audit` alone - no lockfile provenance check.",
      "guide": "script-contract"
    },
    {
      "id": "script.check-ci",
      "kind": "package-script",
      "target": "check:ci",
      "profile": "checks",
      "neededBy": "check-code.yml (actionlint, shellcheck)",
      "toggle": "check-code.yml:actionlint",
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Without a \"check:ci\" script, shared-workflows runs its own scripts/ci/lint-ci.sh. That one already skips cleanly when you have neither scripts/ nor .github/workflows/, so this is the gentlest of the fallbacks.",
      "guide": "script-contract"
    },
    {
      "id": "script.check-secrets",
      "kind": "package-script",
      "target": "check:secrets",
      "profile": "checks",
      "neededBy": "check-code.yml (secret-scan)",
      "toggle": "check-code.yml:secret-scan",
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Without a \"check:secrets\" script, shared-workflows runs its own scripts/checks/secrets.sh: gitleaks over your whole git history with its default rules and any .gitleaks.toml at your root. Ship the script when you want the same scan as a make target, or pass secret-scan: false.",
      "guide": "script-contract"
    },
    {
      "id": "script.i18n-check",
      "kind": "package-script",
      "target": "i18n:check",
      "profile": "checks",
      "neededBy": "check-code.yml (i18n)",
      "toggle": "check-code.yml:i18n",
      "defaultOn": false,
      "severity": "degrades",
      "fix": "Without an \"i18n:check\" script, shared-workflows runs its own scripts/checks/i18n.sh, which calls your \"i18n:extract\" and asserts no diff.",
      "guide": "script-contract"
    },
    {
      "id": "script.codegen-check",
      "kind": "package-script",
      "target": "codegen:check",
      "profile": "checks",
      "neededBy": "check-code.yml (graphql-codegen)",
      "toggle": "check-code.yml:graphql-codegen",
      "defaultOn": false,
      "severity": "degrades",
      "fix": "Without a \"codegen:check\" script, shared-workflows runs its own scripts/checks/codegen.sh, which calls your \"codegen\" and asserts no diff.",
      "guide": "script-contract"
    },
    {
      "id": "script.check-prebuild",
      "kind": "package-script",
      "target": "check-prebuild",
      "profile": "checks",
      "neededBy": "check-code.yml (prebuild-check)",
      "toggle": "check-code.yml:prebuild-check",
      "defaultOn": false,
      "severity": "required",
      "fix": "Add a \"check-prebuild\" script, or leave prebuild-check off. It is minutes rather than seconds, which is why it is off by default.",
      "guide": "script-contract"
    },
    {
      "id": "script.check-bundle-secrets",
      "kind": "package-script",
      "target": "check:bundle-secrets",
      "profile": "checks",
      "neededBy": "check-code.yml (bundle-secrets)",
      "toggle": "check-code.yml:bundle-secrets",
      "defaultOn": false,
      "severity": "required",
      "fix": "Add a \"check:bundle-secrets\" script, or leave bundle-secrets off.",
      "guide": "script-contract"
    },
    {
      "id": "script.check-release",
      "kind": "package-script",
      "target": "check:release",
      "profile": "checks",
      "neededBy": "check-code.yml (release-checks)",
      "toggle": "check-code.yml:release-checks",
      "defaultOn": false,
      "severity": "required",
      "fix": "Add a \"check:release\" script, or leave release-checks off. Only a consumer with a fastlane release setup ships one.",
      "guide": "script-contract"
    },
    {
      "id": "dep.commitlint",
      "kind": "package-dep",
      "target": "@commitlint/cli",
      "profile": "checks",
      "neededBy": "check-code.yml (commitlint), pr-title.yml",
      "toggle": "check-code.yml:commitlint",
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Without @commitlint/cli as a devDependency, scripts/checks/commitlint.sh falls back to npx with a pinned default config - a network fetch, and your own commitlint.config is not what runs.",
      "guide": "script-contract"
    },
    {
      "id": "file.gemfile",
      "kind": "file",
      "target": ["Gemfile"],
      "profile": "checks",
      "neededBy": "check-code.yml (release-checks) via bundler-cache, and scripts/release/fastlane.sh",
      "toggle": "check-code.yml:release-checks",
      "defaultOn": false,
      "severity": "required",
      "fix": "release-checks sets up Ruby with bundler-cache, which needs a Gemfile and a committed Gemfile.lock.",
      "guide": "check-codeyml"
    },

    {
      "id": "script.test-coverage",
      "kind": "package-script",
      "target": "test:coverage",
      "profile": "unit",
      "neededBy": "check-unit.yml (coverage-script)",
      "toggle": "check-unit.yml:coverage",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add a \"test:coverage\" script, or call check-unit.yml with coverage: false so it runs \"test\" instead.",
      "guide": "check-unityml"
    },
    {
      "id": "script.test-scripts",
      "kind": "package-script",
      "target": "test:scripts",
      "profile": "unit",
      "neededBy": "check-unit.yml (scripts-test-script)",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Add a \"test:scripts\" script that tests your own scripts/ directory, or pass scripts-test-script: '' in your caller - an empty value skips the step entirely.",
      "guide": "check-unityml"
    },
    {
      "id": "gate.make-ci-reaches-ci",
      "label": "make ci reaches every CI gate",
      "kind": "make-ci-reaches-ci",
      "target": "ci",
      "profile": "checks",
      "neededBy": "check-code.yml and check-unit.yml, against your Makefile",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Every package script the check-code and check-unit workflows run for your caller must be reachable from `make ci`, by name or through a target of the dashed name (check:docs is make check-docs). Add the gate to a target `make ci` reaches, or switch it off in your caller. No Makefile, or no ci target, and this is skipped.",
      "guide": "the-contract-check"
    },
    {
      "id": "gate.ci-runs-make-ci",
      "label": "CI runs every make ci gate",
      "kind": "ci-runs-make-ci",
      "target": "ci",
      "profile": "checks",
      "neededBy": "your Makefile, against check-code.yml and check-unit.yml",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Every target `make ci` reaches that has a recipe of its own must be run by CI: named like a script CI runs (check-docs for check:docs), or running only pnpm scripts CI runs. Fold a local-only gate into a script CI already runs, or turn its CI toggle on. Otherwise a green `make ci` claims checks CI never makes.",
      "guide": "the-contract-check"
    },

    {
      "id": "file.expo-config",
      "kind": "file",
      "target": ["app.config.ts", "app.config.js", "app.config.cjs", "app.json"],
      "profile": "e2e",
      "neededBy": "check-e2e.yml and every build workflow, through scripts/lib/expo-config.sh",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "These workflows are Expo-specific. The app name, scheme, iOS bundle identifier and Android package are read with `expo config --json`, so an Expo config has to resolve.",
      "guide": "check-e2eyml"
    },
    {
      "id": "dir.maestro",
      "kind": "dir-nonempty",
      "target": ".maestro",
      "profile": "e2e",
      "neededBy": "check-e2e.yml (maestro-flows)",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Add at least one Maestro flow under .maestro/, or point maestro-flows at the directory you use. An empty selection is not a pass: the suite asserts it actually ran flows.",
      "guide": "check-e2eyml"
    },
    {
      "id": "caller.e2e-hooks",
      "kind": "caller-path",
      "target": ["check-e2e.yml:e2e-setup-script", "check-e2e.yml:e2e-teardown-script"],
      "profile": "e2e",
      "neededBy": "check-e2e.yml, through scripts/e2e/run-hook.sh",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "A hook input that names a file which does not exist is fatal - deliberately, because a silently skipped setup hook produces a confusing suite failure later. Leave the input empty if you ship no hook.",
      "guide": "the-e2e-hooks-contract"
    },

    {
      "id": "script.build-web",
      "kind": "package-script",
      "target": "build:web",
      "profile": "web",
      "neededBy": "build-web.yml (export-script)",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Add a \"build:web\" script that writes dist/index.html, or pass a different export-script.",
      "guide": "build-webyml"
    },
    {
      "id": "script.test-e2e-web",
      "kind": "package-script",
      "target": "test:e2e:web",
      "profile": "web",
      "neededBy": "build-web.yml (e2e-script)",
      "toggle": "build-web.yml:playwright",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add a \"test:e2e:web\" script that honours PLAYWRIGHT_SKIP_EXPORT=1 and tests the existing dist/ export rather than making its own, or pass playwright: false.",
      "guide": "the-playwright--export-contract"
    },
    {
      "id": "dep.playwright",
      "kind": "package-dep",
      "target": "@playwright/test",
      "profile": "web",
      "neededBy": "build-web.yml, through scripts/web/playwright-version.sh",
      "toggle": "build-web.yml:playwright",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add @playwright/test as a devDependency. The browser download is keyed on the version resolved from your lockfile, so it is read there before any install.",
      "guide": "build-webyml"
    },

    {
      "id": "script.badges-render",
      "kind": "package-script",
      "target": "badges:render",
      "profile": "badges",
      "neededBy": "publish-badges.yml (render-script)",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Add a \"badges:render\" script that reads BADGE_OUT_DIR, BADGE_UNIT, BADGE_E2E, BADGE_UNIT_LABEL and BADGE_E2E_LABEL and writes into coverage/badge, or stop calling publish-badges.yml.",
      "guide": "publish-badgesyml"
    },

    {
      "id": "file.codeql-config",
      "kind": "file",
      "target": [".github/codeql/codeql-config.yml"],
      "profile": "codeql",
      "neededBy": "check-codeql.yml (config-file)",
      "toggle": null,
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Add .github/codeql/codeql-config.yml, or pass config-file: '' in your caller. Without it the analysis still runs, on CodeQL's own defaults - so your path exclusions and the alert-suppression pack that makes an inline // codeql[rule-id] marker count are not applied.",
      "guide": "check-codeqlyml"
    },

    {
      "id": "script.security-config",
      "kind": "file",
      "target": ["scripts/security/config.mjs"],
      "profile": "security",
      "neededBy": "check-security.yml (the Config job), through scripts/security/settings.sh",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Add scripts/security/config.mjs, or stop calling check-security.yml. It resolves security-policy.json (and its environment twins) into the settings the Config job publishes as outputs for every other job.",
      "guide": "check-securityyml"
    },
    {
      "id": "script.security-deps",
      "kind": "file",
      "target": ["scripts/security/deps.sh"],
      "profile": "security",
      "neededBy": "check-security.yml (deps), through scripts/security/run-job.sh",
      "toggle": "check-security.yml:deps",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add scripts/security/deps.sh, or pass deps: false in your caller. Shared ships no fallback runner - a job that is switched on but whose script is missing fails, by name, rather than skipping quietly.",
      "guide": "check-securityyml"
    },
    {
      "id": "script.security-code",
      "kind": "file",
      "target": ["scripts/security/code.sh"],
      "profile": "security",
      "neededBy": "check-security.yml (code), through scripts/security/run-job.sh",
      "toggle": "check-security.yml:code",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add scripts/security/code.sh, or pass code: false in your caller. Shared ships no fallback runner - a job that is switched on but whose script is missing fails, by name, rather than skipping quietly.",
      "guide": "check-securityyml"
    },
    {
      "id": "script.security-policy-scanner",
      "kind": "file",
      "target": ["scripts/security/policy.sh"],
      "profile": "security",
      "neededBy": "check-security.yml (policy), through scripts/security/run-job.sh",
      "toggle": "check-security.yml:policy",
      "defaultOn": true,
      "severity": "required",
      "fix": "Add scripts/security/policy.sh, or pass policy: false in your caller. Shared ships no fallback runner - a job that is switched on but whose script is missing fails, by name, rather than skipping quietly.",
      "guide": "check-securityyml"
    },
    {
      "id": "script.security-verdict",
      "kind": "file",
      "target": ["scripts/security/verdict.mjs"],
      "profile": "security",
      "neededBy": "check-security.yml (the Verdict job), through scripts/security/verdict.sh",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Add scripts/security/verdict.mjs, or stop calling check-security.yml. It merges every scanner's SARIF and decides the outcome against the resolved severity and failOn.",
      "guide": "check-securityyml"
    },
    {
      "id": "file.security-policy",
      "kind": "file",
      "target": ["security-policy.json"],
      "profile": "security",
      "neededBy": "scripts/security/config.mjs, the settings resolver the Config job runs",
      "toggle": null,
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Add security-policy.json to set severity, failOn and per-job enabled flags. Without it the built-in defaults apply.",
      "guide": "check-securityyml"
    },

    {
      "id": "dir.fastlane",
      "kind": "file",
      "target": ["fastlane/Fastfile"],
      "profile": "release",
      "neededBy": "build-ios.yml, build-android.yml, publish-store.yml",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "The release workflows run named fastlane lanes. Ship a fastlane/Fastfile whose before_all asserts APP_VERSION, APP_BUILD_NUMBER, IOS_BUNDLE_ID, IOS_SCHEME and ANDROID_PACKAGE and rejects an empty value - that assertion is the only thing standing between an unset repository variable and a build signed with an empty identifier.",
      "guide": "the-five-fastfile-contract-variables"
    },
    {
      "id": "lane.ios-build",
      "kind": "fastlane-lane",
      "target": ["ios:build", "ios:verify", "android:build", "android:verify"],
      "profile": "release",
      "neededBy": "build-ios.yml, build-android.yml",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Define the four build lanes. A Fastfile that exists but lacks a lane fails inside fastlane with 'Could not find lane', after prebuild and pod install have already been paid for.",
      "guide": "build-iosyml"
    },
    {
      "id": "lane.app-review-env",
      "label": "APP_REVIEW_* names the lanes read",
      "kind": "fastlane-env-subset",
      "target": ["APP_REVIEW_DEMO_PASSWORD", "APP_REVIEW_DEMO_USER", "APP_REVIEW_EMAIL", "APP_REVIEW_FIRST_NAME", "APP_REVIEW_LAST_NAME", "APP_REVIEW_NOTES", "APP_REVIEW_PHONE"],
      "prefix": "APP_REVIEW_",
      "profile": "release",
      "neededBy": "publish-store.yml, which passes exactly these as secrets",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Your lanes read an APP_REVIEW_* environment variable that publish-store.yml does not pass, so it is always empty on a runner and fastlane submits the empty value. Rename it to one of the listed names, or propose the new secret in shared-workflows first.",
      "guide": "the-contract-check"
    },
    {
      "id": "script.verify-ios",
      "kind": "file",
      "target": ["scripts/release/verify-ios.sh"],
      "profile": "release",
      "neededBy": "the ios verify lane",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Ship scripts/release/verify-ios.sh <ipa-or-app> [--no-signing]. The verify lane calls it by path and fails without it.",
      "guide": "consumer-side-release-scripts"
    },
    {
      "id": "script.verify-android",
      "kind": "file",
      "target": ["scripts/release/verify-android.sh"],
      "profile": "release",
      "neededBy": "the android verify lane",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Ship scripts/release/verify-android.sh <aab> <apk> [--cert-sha256 X]. The verify lane calls it by path and fails without it.",
      "guide": "consumer-side-release-scripts"
    },
    {
      "id": "script.notes",
      "kind": "file",
      "target": ["scripts/release/notes.mjs"],
      "profile": "release",
      "neededBy": "build-prepare.yml, through scripts/release/notes.sh",
      "toggle": null,
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Without scripts/release/notes.mjs the run warns and falls back to an empty store-notes.json and the commit subjects as notes - so your store listings get generic release notes rather than written ones.",
      "guide": "consumer-side-release-scripts"
    },
    {
      "id": "dep.fingerprint",
      "kind": "package-dep",
      "target": "@expo/fingerprint",
      "profile": "release",
      "neededBy": "build-prepare.yml and publish-ota.yml, through scripts/lib/release-env.sh",
      "toggle": null,
      "defaultOn": true,
      "severity": "required",
      "fix": "Add @expo/fingerprint as a devDependency. The native fingerprints go into build-info.json and are what the OTA gate compares - an update whose fingerprint differs from the installed binary crashes every user on the channel.",
      "guide": "the-ota-fingerprint-gate"
    },

    {
      "id": "ignores.biome",
      "kind": "ignores-workflows",
      "target": "biome.json",
      "profile": "checks",
      "neededBy": "your own lint gate, which walks the whole tree",
      "toggle": "check-code.yml:lint",
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Add \"!**/.workflows\" to files.includes in biome.json. Every job checks this repository out into .workflows/, and a tool that globs the whole tree will lint files you do not own.",
      "guide": "workflows-ignore-list-for-consumers"
    },
    {
      "id": "ignores.eslint",
      "kind": "ignores-workflows",
      "target": "eslint.config.mjs",
      "profile": "checks",
      "neededBy": "your own lint gate, which walks the whole tree",
      "toggle": "check-code.yml:lint",
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Add '.workflows/**' to the flat-config ignores array in eslint.config.mjs.",
      "guide": "workflows-ignore-list-for-consumers"
    },
    {
      "id": "ignores.tsconfig",
      "kind": "ignores-workflows",
      "target": "tsconfig.json",
      "profile": "checks",
      "neededBy": "your own typecheck gate",
      "toggle": "check-code.yml:typecheck",
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Add \".workflows\" to exclude in tsconfig.json.",
      "guide": "workflows-ignore-list-for-consumers"
    },
    {
      "id": "ignores.knip",
      "kind": "ignores-workflows-or-narrow",
      "target": "knip.json",
      "profile": "checks",
      "neededBy": "your own knip gate",
      "toggle": "check-code.yml:knip",
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Add \".workflows/**\" to ignore in knip.json, or keep every project/entry glob rooted so none of them reaches into it. The second is what the template does, and it is why this row is satisfied there without an ignore entry.",
      "guide": "workflows-ignore-list-for-consumers"
    },
    {
      "id": "ignores.typos",
      "kind": "ignores-workflows",
      "target": "typos.toml",
      "profile": "checks",
      "neededBy": "your own spell gate",
      "toggle": "check-code.yml:spell",
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Add \".workflows/\" to [files] extend-exclude in typos.toml.",
      "guide": "workflows-ignore-list-for-consumers"
    },
    {
      "id": "ignores.jest",
      "kind": "ignores-workflows",
      "target": "jest.config.ts",
      "profile": "unit",
      "neededBy": "check-unit.yml, which runs your test script over the whole tree",
      "toggle": null,
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Add '/\\\\.workflows/' to testPathIgnorePatterns in jest.config.ts. This repository ships its own *.test.mjs files, and a jest-expo project will collect them and die on import.meta - a red Unit job over a file you do not own.",
      "guide": "workflows-ignore-list-for-consumers"
    },
    {
      "id": "ignores.git",
      "kind": "ignores-workflows",
      "target": ".gitignore",
      "profile": "checks",
      "neededBy": "your own working tree",
      "toggle": null,
      "defaultOn": true,
      "severity": "degrades",
      "fix": "Add /.workflows to .gitignore. The setup action writes .git/info/exclude instead, which is local-only, so this is a convenience rather than a requirement - but without it a local .workflows/ checkout shows up as untracked.",
      "guide": "workflows-ignore-list-for-consumers"
    }
  ]
}
